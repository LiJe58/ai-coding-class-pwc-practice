{
  "name": "internal-control-practice-02-controls-persisted",
  "private": true,
  "scripts": {
    "setup": "python tools/setup_workspace.py",
    "check": "python tools/run_python.py -m compileall -q backend && python tools/run_python.py -m unittest discover -s backend/tests -t backend -p test_checkpoint.py && npm --prefix frontend run check",
    "start:backend": "python tools/run_python.py -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8000",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
