# 00 · Starter

완료 상태: health API, `실습 준비 완료` 화면, 합성 CSV 6종. Day 1–3 업무 기능은 없습니다.

다음 교재: `environment-and-inputs` → `separate-input-errors`.

```text
npm run setup
npm run check
npm run start:backend
npm run dev:frontend
```

기대값은 `/api/health`의 `ready`, CSV 6종, `vendor_changes.csv` 30행입니다. `input/day-1`은 보존합니다. 문제가 생기면 저장소 루트에서 `python scripts/checkpoint.py reset student/00-starter`로 workspace를 복구합니다.
